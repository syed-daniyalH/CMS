// ** MUI imports
import { styled } from "@mui/material/styles"
import Box from "@mui/material/Box";

const PDFRibbonWrapper = styled(Box)(({ theme }) => ({
  display: 'flex',
  justifyContent: 'left',
  position: 'relative'
}))

export default PDFRibbonWrapper
